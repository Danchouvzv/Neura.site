# 🚀 Инструкция по деплою NEURA FTC Platform

## Подготовка к деплою

### 1. Проверьте переменные окружения

Убедитесь, что у вас есть файл `.env.local` с необходимыми переменными:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
GEMINI_API_KEY=your_gemini_api_key (опционально)
```

### 2. Соберите проект локально

```bash
npm install
npm run build
```

Проверьте, что папка `dist` создалась без ошибок.

---

## 🌐 Варианты деплоя

### Вариант 1: Vercel (Рекомендуется)

#### Через веб-интерфейс:

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub/GitLab/Bitbucket
3. Нажмите "Add New Project"
4. Импортируйте ваш репозиторий
5. Настройки:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. Добавьте переменные окружения в Settings → Environment Variables:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `GEMINI_API_KEY` (если используется)
7. Нажмите "Deploy"

#### Через CLI:

```bash
npm i -g vercel
vercel login
vercel
```

Следуйте инструкциям. Vercel автоматически определит настройки из `vercel.json`.

---

### Вариант 2: Netlify

#### Через веб-интерфейс:

1. Зайдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub/GitLab/Bitbucket
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите репозиторий
5. Настройки:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. Добавьте переменные окружения в Site settings → Environment variables
7. Нажмите "Deploy site"

#### Через CLI:

```bash
npm i -g netlify-cli
netlify login
netlify deploy --prod
```

---

### Вариант 3: Cloudflare Pages

1. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Pages → Create a project
3. Подключите репозиторий
4. Настройки:
   - **Framework preset**: Vite
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
5. Добавьте переменные окружения
6. Сохраните и деплойте

---

### Вариант 4: GitHub Pages

1. Установите `gh-pages`:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://yourusername.github.io/ftc-teamhub"
}
```

3. Обновите `vite.config.ts`:
```typescript
export default defineConfig({
  base: '/ftc-teamhub/', // имя вашего репозитория
  // ... остальные настройки
});
```

4. Деплойте:
```bash
npm run deploy
```

---

## ⚙️ Важные настройки

### Переменные окружения

**ВСЕГДА** добавляйте переменные окружения в настройках платформы деплоя. Никогда не коммитьте `.env.local` в репозиторий!

### CORS настройки Supabase

Убедитесь, что в настройках Supabase добавлен домен вашего деплоя в список разрешенных URL.

### Build оптимизация

Проект уже настроен для production build. Vite автоматически:
- Минифицирует код
- Оптимизирует изображения
- Разделяет код на chunks
- Tree-shaking

---

## 🌐 Подключение собственного домена

### Vercel

1. **В настройках проекта:**
   - Зайдите в ваш проект на Vercel
   - Settings → Domains
   - Введите ваш домен (например, `neura.ftc` или `www.neura.ftc`)
   - Нажмите "Add"

2. **Настройка DNS:**
   Vercel покажет вам DNS записи, которые нужно добавить:
   
   **Вариант A: Через A-записи (проще):**
   ```
   Type: A
   Name: @
   Value: 76.76.21.21 (или другой IP от Vercel)
   ```
   
   **Вариант B: Через CNAME (рекомендуется для поддоменов):**
   ```
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```

3. **Где добавить DNS записи:**
   - Зайдите в панель управления вашего регистратора домена
   - Найдите раздел "DNS Management" / "DNS Settings" / "Управление DNS"
   - Добавьте записи, которые показал Vercel
   - Сохраните изменения

4. **Ожидание:**
   - DNS изменения могут занять от нескольких минут до 48 часов
   - Обычно это занимает 1-2 часа
   - Vercel автоматически выдаст SSL сертификат после проверки DNS

### Netlify

1. **В настройках проекта:**
   - Зайдите в ваш проект на Netlify
   - Site settings → Domain management
   - Нажмите "Add custom domain"
   - Введите ваш домен

2. **Настройка DNS:**
   
   **Для корневого домена (example.com):**
   ```
   Type: A
   Name: @
   Value: 75.2.60.5
   ```
   
   **Для поддомена (www.example.com):**
   ```
   Type: CNAME
   Name: www
   Value: your-site.netlify.app
   ```

3. **Netlify DNS (альтернатива):**
   - Можно использовать Netlify DNS вместо DNS вашего регистратора
   - В Domain management → DNS → Add DNS record
   - Netlify автоматически настроит все записи

### Cloudflare Pages

1. **В настройках проекта:**
   - Зайдите в Cloudflare Dashboard
   - Pages → ваш проект → Custom domains
   - Add custom domain → введите домен

2. **Если домен уже в Cloudflare:**
   - DNS записи добавятся автоматически
   - SSL сертификат выдастся автоматически

3. **Если домен не в Cloudflare:**
   - Добавьте домен в Cloudflare (Add Site)
   - Измените nameservers у регистратора на те, что даст Cloudflare
   - После этого настройте домен в Pages

---

## 🔧 Настройка DNS у популярных регистраторов

### Namecheap

1. Зайдите в Domain List → Manage
2. Advanced DNS
3. Добавьте записи:
   - A Record: `@` → IP адрес от платформы
   - CNAME Record: `www` → cname.vercel-dns.com (для Vercel)

### GoDaddy

1. My Products → DNS
2. Add → выберите тип записи
3. Добавьте записи

### Google Domains

1. DNS → Custom records
2. Add record → выберите тип
3. Добавьте записи

### REG.RU / Timeweb / другие российские регистраторы

1. Управление доменом → DNS-серверы / DNS-зона
2. Добавьте A-запись для корневого домена
3. Добавьте CNAME для www

---

## ✅ Проверка настройки домена

После добавления DNS записей:

1. **Проверьте DNS:**
   ```bash
   # В терминале
   nslookup your-domain.com
   dig your-domain.com
   ```

2. **Проверьте на платформе:**
   - Vercel/Netlify покажут статус "Valid Configuration" когда DNS настроен правильно
   - Обычно это занимает 1-2 часа

3. **Проверьте SSL:**
   - Платформы автоматически выдают SSL сертификаты
   - После настройки DNS подождите еще 10-30 минут для SSL

---

## 🔍 Проверка после деплоя

1. ✅ Проверьте, что главная страница загружается
2. ✅ Проверьте навигацию (Map, Q&A, TeamHub)
3. ✅ Проверьте авторизацию (Sign In/Sign Up)
4. ✅ Проверьте работу Supabase (Q&A, TeamHub)
5. ✅ Проверьте на мобильных устройствах

---

## 🐛 Решение проблем

### Ошибка 404 на роутах

Убедитесь, что настроены redirects/rewrites для SPA (уже есть в `vercel.json` и `netlify.toml`).

### Переменные окружения не работают

- Убедитесь, что переменные начинаются с `VITE_` для Vite
- Перезапустите build после добавления переменных
- Проверьте, что переменные добавлены в правильном окружении (Production)

### Ошибки сборки

```bash
# Очистите кэш и пересоберите
rm -rf node_modules dist
npm install
npm run build
```

---

## 📝 Чеклист перед деплоем

- [ ] Все переменные окружения добавлены
- [ ] `npm run build` выполняется без ошибок
- [ ] Тестирование на `npm run preview`
- [ ] Supabase CORS настроен
- [ ] `.env.local` в `.gitignore`
- [ ] README обновлен (если нужно)

---

## 🎉 Готово!

После успешного деплоя вы получите URL вида:
- Vercel: `https://your-project.vercel.app`
- Netlify: `https://your-project.netlify.app`
- Cloudflare: `https://your-project.pages.dev`

Удачи! 🚀

