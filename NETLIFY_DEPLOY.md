# 🚀 Деплой на Netlify

## Быстрая инструкция

### Вариант 1: Через веб-интерфейс (Рекомендуется)

#### Шаг 1: Подготовка
✅ Проект уже в GitHub: https://github.com/Danchouvzv/neura-ftcc
✅ Файл `netlify.toml` создан и настроен

#### Шаг 2: Деплой на Netlify

1. **Зайдите на [netlify.com](https://netlify.com)**
   - Войдите через GitHub/GitLab/Bitbucket

2. **Add new site → Import an existing project**
   - Нажмите "Add new site"
   - Выберите "Import an existing project"
   - Авторизуйтесь через GitHub

3. **Выберите репозиторий**
   - Найдите и выберите: `Danchouvzv/neura-ftcc`
   - Нажмите "Connect"

4. **Настройки сборки (автоматически определятся из netlify.toml):**
   - **Build command**: `npm run build` ✅
   - **Publish directory**: `dist` ✅
   - **Branch to deploy**: `main` ✅

5. **Добавьте переменные окружения:**
   - Нажмите "Show advanced"
   - Или после создания сайта: Site settings → Environment variables
   - Добавьте:
     ```
     VITE_SUPABASE_URL = ваш_supabase_url
     VITE_SUPABASE_ANON_KEY = ваш_supabase_anon_key
     GEMINI_API_KEY = ваш_gemini_key (опционально)
     ```

6. **Deploy site!**
   - Нажмите "Deploy site"
   - Дождитесь завершения сборки (обычно 2-3 минуты)

7. **После деплоя:**
   - Netlify даст вам URL: `https://your-site-name.netlify.app`
   - Добавьте этот URL в Supabase CORS:
     - Supabase Dashboard → Settings → API
     - Additional Allowed Origins → добавьте URL

---

### Вариант 2: Через Netlify CLI

#### Шаг 1: Установка Netlify CLI

```bash
npm install -g netlify-cli
```

#### Шаг 2: Вход в Netlify

```bash
netlify login
```

Откроется браузер для авторизации.

#### Шаг 3: Инициализация проекта

```bash
netlify init
```

Следуйте инструкциям:
- Выберите "Create & configure a new site"
- Выберите команду (оставьте по умолчанию)
- Выберите директорию публикации: `dist`

#### Шаг 4: Добавление переменных окружения

```bash
# Добавить переменную
netlify env:set VITE_SUPABASE_URL "your_supabase_url"
netlify env:set VITE_SUPABASE_ANON_KEY "your_supabase_anon_key"
netlify env:set GEMINI_API_KEY "your_gemini_key"
```

Или через веб-интерфейс: Site settings → Environment variables

#### Шаг 5: Деплой

```bash
# Preview деплой
netlify deploy

# Production деплой
netlify deploy --prod
```

---

## 🌐 Добавление собственного домена

### Через веб-интерфейс:

1. **Site settings → Domain management**
2. **Add custom domain**
3. **Введите ваш домен** (например: `neura.ftc`)
4. **Настройте DNS записи:**

   **Для корневого домена (neura.ftc):**
   ```
   Type: A
   Name: @
   Value: 75.2.60.5
   ```

   **Для поддомена www (www.neura.ftc):**
   ```
   Type: CNAME
   Name: www
   Value: your-site-name.netlify.app
   ```

5. **Добавьте записи в панели управления регистратора**
6. **Подождите 1-2 часа** для применения DNS
7. **SSL сертификат** выдастся автоматически

### Через Netlify DNS (альтернатива):

1. **Domain management → DNS**
2. **Add DNS record**
3. Netlify автоматически настроит все записи
4. Измените nameservers у регистратора на те, что даст Netlify

---

## ⚙️ Настройки после деплоя

### 1. Supabase CORS

Зайдите в Supabase Dashboard:
- Settings → API
- Additional Allowed Origins
- Добавьте:
  - `https://your-site-name.netlify.app`
  - `https://your-domain.com` (если добавили домен)

### 2. Проверка работы

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Навигация работает (Map, Q&A, TeamHub)
- ✅ Авторизация работает (Sign In/Sign Up)
- ✅ Supabase подключен (Q&A, TeamHub)

---

## 🔧 Конфигурация (netlify.toml)

Файл `netlify.toml` уже настроен:

```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

Это обеспечивает:
- Правильную сборку проекта
- SPA роутинг (все роуты перенаправляются на index.html)

---

## 🐛 Решение проблем

### Build ошибки

1. **Проверьте логи в Netlify Dashboard:**
   - Site → Deploys → выберите деплой → View build log

2. **Очистите кэш:**
   - Site settings → Build & deploy → Clear cache and retry deploy

3. **Локальная проверка:**
   ```bash
   npm install
   npm run build
   ```

### Переменные окружения не работают

- Убедитесь, что переменные начинаются с `VITE_`
- Проверьте, что они добавлены в Production окружении
- Передеплойте после добавления переменных

### 404 на роутах

Уже настроено в `netlify.toml` - все роуты перенаправляются на `index.html`

### Домен не работает

1. Проверьте DNS записи через `nslookup your-domain.com`
2. Убедитесь, что домен добавлен в Netlify
3. Подождите 1-2 часа для применения DNS

---

## 📊 Мониторинг и аналитика

### Netlify Analytics

1. Site settings → Analytics
2. Включите Netlify Analytics (если нужно)
3. Просматривайте статистику посещений

### Логи

- Site → Functions → Logs (для serverless функций)
- Site → Deploys → View build log (для логов сборки)

---

## ✅ Чеклист

- [ ] Репозиторий подключен к Netlify
- [ ] Build command: `npm run build`
- [ ] Publish directory: `dist`
- [ ] Переменные окружения добавлены
- [ ] Деплой выполнен успешно
- [ ] Supabase CORS настроен
- [ ] Сайт проверен
- [ ] Домен добавлен (если нужно)

---

## 🎉 Готово!

После успешного деплоя ваш сайт будет доступен по адресу:
- **Netlify URL**: `https://your-site-name.netlify.app`
- **Custom domain**: `https://your-domain.com` (после настройки)

Удачи! 🚀

