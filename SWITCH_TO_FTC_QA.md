# Переключение на проект ftc-qa в Supabase

## 📋 Шаг 1: Получить ключи проекта ftc-qa

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект **ftc-qa**
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon/public key** (начинается с `sb_publishable_` или `eyJ`)

## 📝 Шаг 2: Обновить .env.local

Обновите файл `.env.local` с ключами проекта `ftc-qa`:

```env
VITE_SUPABASE_URL=https://YOUR_FTC_QA_PROJECT_URL.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_FTC_QA_ANON_KEY
```

## 🗄️ Шаг 3: Применить SQL миграцию

1. В проекте **ftc-qa** откройте **SQL Editor**
2. Откройте файл `supabase/migrations/001_initial_schema.sql`
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor
5. Нажмите **Run**

## ⚠️ Важно

- Если в проекте `ftc-qa` уже есть таблицы для Q&A (`questions`, `answers`, `users`), они не будут затронуты
- Таблицы TeamHub будут созданы отдельно и не конфликтуют с Q&A таблицами
- Оба функционала (Q&A и TeamHub) могут работать в одном проекте Supabase

## 🔄 После обновления

1. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. Проверьте консоль браузера - должно быть:
   ```
   ✅ Supabase client initialized successfully
   🔑 Key type: Publishable (anon) ✅
   ```

## 📊 Структура базы данных

После применения миграции в проекте `ftc-qa` будут:

**Q&A таблицы (если уже есть):**
- `questions`
- `answers`
- `users`

**TeamHub таблицы (новые):**
- `teams`
- `members`
- `tasks`
- `ideas`
- `calendar_events`
- `invitations`
- `team_activities`

Все таблицы могут сосуществовать в одном проекте!

