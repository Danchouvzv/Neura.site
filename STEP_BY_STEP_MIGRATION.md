# 📋 Пошаговая инструкция применения миграции

## ✅ Шаг 1: Вы уже сделали!
Вы удалили старую таблицу `members`:
```sql
DROP TABLE IF EXISTS members CASCADE;
```
**Результат:** ✅ Success, no rows returned

## 📝 Шаг 2: Применить основную миграцию

Теперь нужно применить миграцию `001_initial_schema.sql`, которая создаст все таблицы с правильной структурой.

### Что делать:

1. **Откройте Supabase Dashboard:**
   - Перейдите на https://app.supabase.com
   - Выберите проект **ftc-qa**

2. **Откройте SQL Editor:**
   - В левом меню найдите **SQL Editor**
   - Нажмите **New query**

3. **Откройте файл миграции:**
   - В вашем проекте откройте файл: `supabase/migrations/001_initial_schema.sql`
   - Выделите **ВЕСЬ** текст (Ctrl+A / Cmd+A)
   - Скопируйте (Ctrl+C / Cmd+C)

4. **Вставьте в SQL Editor:**
   - Вставьте скопированный код в SQL Editor в Supabase
   - Нажмите **Run** (или `Ctrl+Enter` / `Cmd+Enter`)

## ✅ Шаг 3: Проверка

После выполнения миграции проверьте:

1. **В Supabase Dashboard:**
   - Перейдите в **Table Editor**
   - Должны появиться таблицы:
     - ✅ `teams`
     - ✅ `members` (с колонкой `user_id`)
     - ✅ `tasks`
     - ✅ `ideas`
     - ✅ `calendar_events`
     - ✅ `invitations`
     - ✅ `team_activities`

2. **Проверьте структуру таблицы `members`:**
   - Откройте таблицу `members`
   - Должны быть колонки:
     - `id`
     - `team_id`
     - `user_id` ✅ (это важно!)
     - `role`
     - `created_at`
     - `updated_at`

## 🎉 Готово!

После успешного применения миграции:
1. Перезапустите dev сервер: `npm run dev`
2. Зарегистрируйтесь через `/signup`
3. Создайте команду или присоединитесь к существующей

## ⚠️ Если возникли ошибки

Если при применении миграции возникли ошибки:

1. **"relation already exists"** - таблица уже существует, это нормально для других таблиц (не members)
2. **"column user_id does not exist"** - убедитесь, что таблица members была удалена перед применением миграции
3. **"permission denied"** - проверьте, что вы используете правильный проект

## 📝 Что создаст миграция:

- ✅ Все таблицы TeamHub
- ✅ Связи между таблицами (foreign keys)
- ✅ Индексы для оптимизации
- ✅ Row Level Security (RLS) политики
- ✅ Триггеры для автоматического обновления `updated_at`

