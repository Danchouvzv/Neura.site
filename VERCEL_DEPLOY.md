# 🚀 Быстрый деплой через Vercel

## Вариант 1: Через веб-интерфейс (Проще)

### Шаг 1: Подготовка
✅ Проект собран: `npm run build` выполнен успешно
✅ Все файлы готовы

### Шаг 2: Деплой на Vercel

1. **Зайдите на [vercel.com](https://vercel.com)**
2. **Войдите** через GitHub/GitLab/Bitbucket
3. **Нажмите "Add New Project"**
4. **Импортируйте репозиторий:**
   - Выберите ваш репозиторий
   - Или подключите новый

5. **Настройки проекта:**
   - Framework Preset: **Vite** (автоматически определится)
   - Build Command: `npm run build` (уже настроено)
   - Output Directory: `dist` (уже настроено)
   - Install Command: `npm install` (уже настроено)

6. **Добавьте переменные окружения:**
   - Settings → Environment Variables
   - Добавьте:
     ```
     VITE_SUPABASE_URL = ваш_supabase_url
     VITE_SUPABASE_ANON_KEY = ваш_supabase_anon_key
     GEMINI_API_KEY = ваш_gemini_key (опционально)
     ```

7. **Нажмите "Deploy"**

8. **После деплоя:**
   - Vercel даст вам URL: `https://your-project.vercel.app`
   - Добавьте этот URL в Supabase CORS (Settings → API → Additional Allowed Origins)

---

## Вариант 2: Через CLI (Быстрее)

### Шаг 1: Войдите в Vercel

```bash
vercel login
```

### Шаг 2: Деплой

```bash
# Первый деплой (preview)
vercel

# Или сразу в production
vercel --prod
```

### Шаг 3: Добавьте переменные окружения

```bash
# Добавить переменную
vercel env add VITE_SUPABASE_URL production
vercel env add VITE_SUPABASE_ANON_KEY production
vercel env add GEMINI_API_KEY production
```

Или через веб-интерфейс: Settings → Environment Variables

### Шаг 4: Передеплойте после добавления переменных

```bash
vercel --prod
```

---

## 🌐 Добавление собственного домена

После успешного деплоя:

1. **Settings → Domains**
2. **Введите ваш домен** (например: `neura.ftc`)
3. **Скопируйте DNS записи**, которые покажет Vercel
4. **Добавьте их в панели управления регистратора:**
   - A запись: `@` → IP адрес (для корневого домена)
   - CNAME: `www` → `cname.vercel-dns.com` (для www)
5. **Подождите 1-2 часа** для применения DNS
6. **SSL сертификат** выдастся автоматически

Подробнее: см. `CUSTOM_DOMAIN.md`

---

## ⚙️ Важные настройки после деплоя

### 1. Supabase CORS

Зайдите в Supabase Dashboard:
- Settings → API
- Additional Allowed Origins
- Добавьте: `https://your-project.vercel.app`
- И ваш домен: `https://your-domain.com`

### 2. Проверка работы

После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Навигация работает (Map, Q&A, TeamHub)
- ✅ Авторизация работает (Sign In/Sign Up)
- ✅ Supabase подключен (Q&A, TeamHub)

---

## 🐛 Решение проблем

### Build ошибки

```bash
# Очистите и пересоберите
rm -rf node_modules dist
npm install
npm run build
```

### Переменные окружения не работают

- Убедитесь, что переменные начинаются с `VITE_`
- Проверьте, что они добавлены в Production окружении
- Передеплойте после добавления переменных

### 404 на роутах

Уже настроено в `vercel.json` - все роуты перенаправляются на `index.html`

---

## ✅ Чеклист

- [ ] Проект собран (`npm run build`)
- [ ] Репозиторий на GitHub/GitLab
- [ ] Vercel аккаунт создан
- [ ] Проект импортирован в Vercel
- [ ] Переменные окружения добавлены
- [ ] Деплой выполнен
- [ ] Supabase CORS настроен
- [ ] Сайт проверен

---

## 🎉 Готово!

Ваш сайт будет доступен по адресу:
- Preview: `https://your-project-git-main-yourusername.vercel.app`
- Production: `https://your-project.vercel.app`
- Custom domain: `https://your-domain.com` (после настройки)

Удачи! 🚀

