# 🔧 Исправление ошибки "Invalid API key" в Vercel

## ❌ Проблема
```
AuthApiError: Invalid API key
Failed to load resource: the server responded with a status of 401
```

Это означает, что переменные окружения Supabase не настроены в Vercel.

---

## ✅ Решение: Добавить переменные окружения в Vercel

### Шаг 1: Найдите ваши Supabase ключи

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Найдите:
   - **Project URL**: `https://wdnutnqyaihhxjnasymr.supabase.co`
   - **anon public** ключ (начинается с `eyJ` или `sb_publishable_`)

### Шаг 2: Добавьте переменные в Vercel

1. **Зайдите в ваш проект на Vercel**
   - Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
   - Выберите проект `neura-ftcc`

2. **Откройте Settings → Environment Variables**

3. **Добавьте переменные:**

   **Переменная 1:**
   - Name: `VITE_SUPABASE_URL`
   - Value: `https://wdnutnqyaihhxjnasymr.supabase.co`
   - Environment: выберите все (Production, Preview, Development)

   **Переменная 2:**
   - Name: `VITE_SUPABASE_ANON_KEY`
   - Value: ваш anon ключ из Supabase (начинается с `eyJ` или `sb_publishable_`)
   - Environment: выберите все (Production, Preview, Development)

   **Переменная 3 (опционально):**
   - Name: `GEMINI_API_KEY`
   - Value: ваш Gemini API ключ (если используете)
   - Environment: выберите все

4. **Сохраните** (нажмите "Save")

### Шаг 3: Передеплойте проект

После добавления переменных **ОБЯЗАТЕЛЬНО** передеплойте:

1. В Vercel Dashboard откройте вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Или создайте новый коммит и пуш в GitHub (автоматический деплой)

---

## 🔍 Проверка

После передеплоя проверьте:

1. ✅ Сайт загружается без ошибок
2. ✅ В консоли браузера нет ошибок "Invalid API key"
3. ✅ Авторизация работает (Sign In / Sign Up)
4. ✅ Q&A страница работает

---

## ⚠️ Важно

- **НЕ используйте SECRET ключ** (начинается с `sb_secret_`)
- **Используйте только ANON (public) ключ** (начинается с `eyJ` или `sb_publishable_`)
- Переменные должны начинаться с `VITE_` для Vite проектов
- После добавления переменных **ОБЯЗАТЕЛЬНО передеплойте**

---

## 🐛 Если ошибка осталась

1. Проверьте, что переменные добавлены для всех окружений (Production, Preview, Development)
2. Убедитесь, что ключ правильный (скопируйте заново из Supabase)
3. Проверьте, что URL правильный: `https://wdnutnqyaihhxjnasymr.supabase.co`
4. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
5. Проверьте консоль браузера на наличие других ошибок

---

## 📝 Быстрая команда через CLI (альтернатива)

Если у вас установлен Vercel CLI:

```bash
vercel env add VITE_SUPABASE_URL production
# Введите: https://wdnutnqyaihhxjnasymr.supabase.co

vercel env add VITE_SUPABASE_ANON_KEY production
# Введите: ваш_anon_ключ

vercel --prod
```

---

✅ После выполнения этих шагов ошибка должна исчезнуть!

