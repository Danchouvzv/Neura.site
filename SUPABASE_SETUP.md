# Настройка Supabase

## ⚠️ ВАЖНО: Типы ключей

### ANON (публичный) ключ - для браузера ✅
- Используется в клиентском коде (браузер)
- Безопасен для публичного использования
- Начинается с `eyJ` (JWT) или `sb_publishable_`
- Находится в: **Settings > API > Project API keys > anon public**

### SECRET (секретный) ключ - только для сервера ❌
- **НИКОГДА не используйте в браузере!**
- Только для серверных приложений
- Начинается с `sb_secret_`
- Находится в: **Settings > API > Project API keys > service_role secret**

## 📋 Инструкция по настройке

### 1. Найдите правильный ANON ключ

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. В разделе **Project API keys** найдите:
   - **`anon` `public`** - это тот ключ, который нужен!
   - Скопируйте его (начинается с `eyJ` или `sb_publishable_`)

### 2. Обновите .env.local

Создайте или обновите файл `.env.local` в корне проекта:

```env
VITE_SUPABASE_URL=https://wdnutnqyaihhxjnasymr.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_anon_ключ_здесь
```

**Пример правильного ключа:**
```env
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndkbnV0bnF5YWloaHhqbmFzeW1yIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MDAwMDAwMDAsImV4cCI6MjAxNTY3NjAwMH0.xxxxx
```

Или если используется новый формат:
```env
VITE_SUPABASE_ANON_KEY=sb_publishable_K16J7b3m90jSKeeiLqz8KQ_pyuJvalM
```

### 3. Перезапустите dev сервер

```bash
npm run dev
```

### 4. Проверьте консоль браузера

Откройте консоль разработчика (F12) и проверьте:
- ✅ `Supabase client initialized successfully`
- ✅ `Supabase connection test passed`

Если видите ошибки:
- ❌ Проверьте формат ключа
- ❌ Убедитесь, что используете ANON ключ, а не SECRET
- ❌ Проверьте, что URL правильный

## 🔧 Решение проблем

### Ошибка 400 Bad Request
- Пользователь не зарегистрирован → сначала зарегистрируйтесь через `/signup`
- Неправильный пароль → проверьте пароль

### Ошибка 429 Too Many Requests
- Слишком много попыток → подождите указанное время (обычно 60 секунд)

### Ошибка при создании профиля в таблице `users`
- Таблица может не существовать → это не критично, пользователь все равно создается
- RLS может блокировать → проверьте настройки Row Level Security в Supabase

## 📝 Текущая конфигурация

**URL:** `https://wdnutnqyaihhxjnasymr.supabase.co`

**Текущий ключ:** `sb_publishable_K16J7b3m90jSKeeiLqz8KQ_pyuJvalM`

Если этот ключ не работает, найдите правильный ANON ключ в Supabase Dashboard.

