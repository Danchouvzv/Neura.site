# 🚀 Применение миграции в проекте ftc-qa

## ✅ Хорошие новости!

Ваша конфигурация уже правильная:
- ✅ URL: `https://wdnutnqyaihhxjnasymr.supabase.co`
- ✅ Anon Key: `sb_publishable_K16J7b3m90jSKeeiLqz8KQ_pyuJvalM`

Проект `ftc-qa` использует те же ключи, что уже настроены в `.env.local`.

## 📋 Шаг 1: Открыть SQL Editor в проекте ftc-qa

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Убедитесь, что выбран проект **ftc-qa** (вверху слева)
3. В левом меню найдите **SQL Editor**
4. Нажмите **New query**

## 📋 Шаг 2: Применить миграцию

1. Откройте файл `supabase/migrations/001_initial_schema.sql` в вашем проекте
2. Скопируйте **весь** SQL код (все 383 строки)
3. Вставьте в SQL Editor в Supabase
4. Нажмите **Run** (или `Cmd/Ctrl + Enter`)

## ✅ Шаг 3: Проверка

После выполнения миграции проверьте:

1. **Table Editor** → должны появиться новые таблицы:
   - ✅ `teams`
   - ✅ `members`
   - ✅ `tasks`
   - ✅ `ideas`
   - ✅ `calendar_events`
   - ✅ `invitations`
   - ✅ `team_activities`

2. **Authentication** → **Policies** → должны быть настроены RLS политики

3. **Консоль браузера** (после перезапуска dev сервера):
   ```
   ✅ Supabase client initialized successfully
   🔑 Key type: Publishable (anon) ✅
   ✅ Supabase connection test passed
   ```

## ⚠️ Важно

- Если в проекте `ftc-qa` уже есть таблицы для Q&A (`questions`, `answers`, `users`), они **не будут затронуты**
- Миграция создаст только новые таблицы для TeamHub
- Оба функционала (Q&A и TeamHub) будут работать вместе в одном проекте

## 🔄 После применения миграции

1. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. Зарегистрируйтесь через `/signup`
3. Создайте команду или присоединитесь к существующей
4. Проверьте, что данные сохраняются в Supabase

## 🐛 Если возникли ошибки

Если при применении миграции возникли ошибки:

1. **"relation already exists"** - таблица уже существует, это нормально, можно пропустить
2. **"permission denied"** - проверьте, что вы используете правильный проект
3. **"syntax error"** - убедитесь, что скопировали весь SQL код полностью

## 📊 Структура после миграции

```
ftc-qa проект:
├── Q&A таблицы (если уже есть):
│   ├── questions
│   ├── answers
│   └── users
│
└── TeamHub таблицы (новые):
    ├── teams
    ├── members
    ├── tasks
    ├── ideas
    ├── calendar_events
    ├── invitations
    └── team_activities
```

Все готово к использованию! 🎉

