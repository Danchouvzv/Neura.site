# 🔧 Исправление проблем с SQL

## ❌ Проблема

В коде используются представления (views) `questions_with_author` и `answers_with_author`, а также таблицы `questions` и `answers`, но они не созданы в базе данных.

## ✅ Решение

Создана новая миграция `005_create_qa_tables.sql`, которая создает:
- ✅ Таблицу `questions` для вопросов
- ✅ Таблицу `answers` для ответов
- ✅ Представления `questions_with_author` и `answers_with_author` с информацией об авторах
- ✅ Индексы для оптимизации запросов
- ✅ RLS политики для безопасности
- ✅ Триггеры для автоматического обновления `updated_at`

## 📋 Инструкция по применению

### Шаг 1: Открыть SQL Editor в Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект (например, `ftc-qa`)
3. Перейдите в **SQL Editor**
4. Нажмите **New query**

### Шаг 2: Применить миграцию

1. Откройте файл `supabase/migrations/005_create_qa_tables.sql` в вашем проекте
2. Скопируйте **весь** SQL код
3. Вставьте в SQL Editor в Supabase
4. Нажмите **Run** (или `Cmd/Ctrl + Enter`)

### Шаг 3: Проверка

После выполнения миграции проверьте:

1. **Table Editor** → должны появиться таблицы:
   - ✅ `questions`
   - ✅ `answers`

2. **SQL Editor** → выполните запрос для проверки представлений:
   ```sql
   SELECT * FROM questions_with_author LIMIT 1;
   SELECT * FROM answers_with_author LIMIT 1;
   ```

3. **Table Editor** → проверьте структуру:
   - `questions` должна иметь колонки: `id`, `author_id`, `title`, `body`, `tags`, `likes`, `created_at`, `updated_at`
   - `answers` должна иметь колонки: `id`, `question_id`, `author_id`, `body`, `likes`, `created_at`, `updated_at`

## 🔍 Что создается

### Таблицы:
- **questions** - хранит вопросы пользователей
- **answers** - хранит ответы на вопросы

### Представления (Views):
- **questions_with_author** - вопросы с информацией об авторе (username, email)
- **answers_with_author** - ответы с информацией об авторе (username, email)

### Индексы:
- `idx_questions_author_id` - для быстрого поиска вопросов по автору
- `idx_questions_created_at` - для сортировки по дате создания
- `idx_questions_tags` - GIN индекс для поиска по тегам
- `idx_answers_question_id` - для быстрого поиска ответов по вопросу
- `idx_answers_author_id` - для быстрого поиска ответов по автору
- `idx_answers_created_at` - для сортировки по дате создания

### RLS Политики:
- ✅ Все могут просматривать вопросы и ответы
- ✅ Только авторизованные пользователи могут создавать вопросы/ответы
- ✅ Пользователи могут редактировать/удалять только свои вопросы/ответы

### Триггеры:
- Автоматическое обновление `updated_at` при изменении записи

## ⚠️ Важно

1. **Порядок миграций**: Убедитесь, что миграция `003_create_users_table.sql` уже применена, так как `questions` и `answers` ссылаются на таблицу `users`.

2. **Если таблицы уже существуют**: Миграция использует `CREATE TABLE IF NOT EXISTS`, поэтому существующие таблицы не будут затронуты. Однако представления будут пересозданы.

3. **Если возникли ошибки**:
   - **"relation users does not exist"** - сначала примените миграцию `003_create_users_table.sql`
   - **"permission denied"** - убедитесь, что вы используете правильный проект и имеете права администратора
   - **"syntax error"** - убедитесь, что скопировали весь SQL код полностью

## 🧪 Тестирование

После применения миграции:

1. **Создайте вопрос** через `/qa/new`
2. **Просмотрите вопросы** на `/qa`
3. **Откройте вопрос** и добавьте ответ
4. **Проверьте**, что автор отображается корректно

## 📊 Структура после миграции

```
База данных:
├── users (из миграции 003)
├── questions (новая)
├── answers (новая)
├── questions_with_author (view, новая)
└── answers_with_author (view, новая)
```

## ✅ После применения

После успешного применения миграции:

1. Перезапустите dev сервер:
   ```bash
   npm run dev
   ```

2. Проверьте консоль браузера - не должно быть ошибок о несуществующих таблицах

3. Протестируйте Q&A функционал:
   - Создание вопроса
   - Просмотр вопросов
   - Добавление ответа
   - Просмотр ответов

Все готово! 🎉

