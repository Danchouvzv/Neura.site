# 🚀 Supabase Integration для TeamHub

## ✅ Что было сделано

### 1. SQL Схема базы данных
Создана полная SQL схема в `supabase/migrations/001_initial_schema.sql`:
- ✅ 7 таблиц: `teams`, `members`, `tasks`, `ideas`, `calendar_events`, `invitations`, `team_activities`
- ✅ Row Level Security (RLS) политики для безопасности
- ✅ Индексы для оптимизации запросов
- ✅ Триггеры для автоматического обновления `updated_at`

### 2. Сервис для работы с Supabase
Создан `services/teamHubService.ts` с функциями:
- ✅ Управление командами (создание, получение, присоединение)
- ✅ Управление участниками
- ✅ Управление задачами (CRUD)
- ✅ Управление идеями (CRUD + голосование)
- ✅ Управление событиями календаря (CRUD)
- ✅ Управление приглашениями (создание, принятие, отклонение)
- ✅ Лог активности команды

### 3. Интеграция в App.tsx
- ✅ Автоматическая загрузка данных из Supabase при входе
- ✅ Синхронизация с localStorage для совместимости
- ✅ Автоматическое сохранение изменений в Supabase
- ✅ Fallback на localStorage если Supabase недоступен

## 📋 Инструкция по настройке

### Шаг 1: Применить SQL миграцию

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект: `wdnutnqyaihhxjnasymr`
3. Перейдите в **SQL Editor**
4. Откройте файл `supabase/migrations/001_initial_schema.sql`
5. Скопируйте весь SQL код
6. Вставьте в SQL Editor
7. Нажмите **Run** (или `Cmd/Ctrl + Enter`)

### Шаг 2: Проверка

После применения миграции проверьте в Supabase Dashboard:
- **Table Editor** → должны быть видны все 7 таблиц
- **Authentication** → **Policies** → должны быть настроены RLS политики

### Шаг 3: Тестирование

1. Зарегистрируйтесь через `/signup`
2. Войдите через `/signin`
3. Создайте команду или присоединитесь к существующей
4. Проверьте, что данные сохраняются в Supabase

## 🔄 Как это работает

### Автоматическая синхронизация

1. **При входе пользователя:**
   - Проверяется сессия Supabase
   - Загружаются данные команды из Supabase
   - Если команды нет, используется localStorage

2. **При создании/изменении данных:**
   - Данные сохраняются в Supabase
   - Также сохраняются в localStorage для совместимости

3. **При ошибках Supabase:**
   - Автоматический fallback на localStorage
   - Приложение продолжает работать

### Структура данных

```
teams (команды)
  ├── members (участники)
  │   ├── tasks (задачи)
  │   ├── ideas (идеи)
  │   ├── calendar_events (события)
  │   └── team_activities (активность)
  └── invitations (приглашения)
```

## 🔐 Безопасность

- ✅ Row Level Security (RLS) включен на всех таблицах
- ✅ Пользователи видят только данные своей команды
- ✅ Капитаны могут управлять командой
- ✅ Пользователи могут обновлять только свои данные

## 📝 Важные заметки

1. **Первый вход:** После регистрации пользователь автоматически создается в `members` при первом входе в TeamHub

2. **Создание команды:** При создании команды автоматически создается запись капитана в `members`

3. **Приглашения:** Приглашения создаются капитаном и отправляются по email

4. **Синхронизация:** Данные автоматически синхронизируются между всеми участниками команды

## 🐛 Отладка

Если что-то не работает:

1. **Проверьте консоль браузера** на ошибки
2. **Проверьте Supabase Logs:** Dashboard → Logs → Postgres Logs
3. **Проверьте RLS политики:** Dashboard → Authentication → Policies
4. **Проверьте авторизацию:** `supabase.auth.getUser()` в консоли

## 📚 Документация

- [Supabase Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Supabase JavaScript Client](https://supabase.com/docs/reference/javascript/introduction)
- [SQL миграции](https://supabase.com/docs/guides/database/migrations)

## 🎯 Следующие шаги

1. ✅ Применить SQL миграцию в Supabase
2. ✅ Протестировать создание команды
3. ✅ Протестировать присоединение к команде
4. ✅ Протестировать создание задач, идей, событий
5. ✅ Проверить синхронизацию между пользователями

