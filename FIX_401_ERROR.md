# 🔧 Исправление ошибки 401 (Unauthorized)

## ❌ Проблема
```
POST https://wdnutnqyaihhxjnasymr.supabase.co/rest/v1/users 401 (Unauthorized)
```

## 🔍 Причина
Ошибка 401 возникает потому что:
1. **Email не подтвержден** - Supabase по умолчанию требует подтверждение email перед использованием
2. **RLS политика блокирует INSERT** - когда email не подтвержден, `auth.uid()` может быть недоступен

## ✅ Решения

### Решение 1: Отключить подтверждение email (рекомендуется для разработки)

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите проект **ftc-qa**
3. Перейдите в **Authentication** → **Settings** → **Email Auth**
4. Найдите **"Enable email confirmations"**
5. **Отключите** эту опцию
6. Нажмите **Save**

После этого пользователи смогут использовать приложение сразу после регистрации.

### Решение 2: Применить миграцию для исправления политики

1. Откройте SQL Editor в Supabase
2. Откройте файл: `supabase/migrations/004_fix_users_insert_policy.sql`
3. Скопируйте весь код
4. Вставьте в SQL Editor
5. Нажмите **Run**

### Решение 3: Создание профиля при входе (уже реализовано)

Профиль будет создаваться автоматически когда пользователь:
1. Подтвердит email
2. Войдет в систему через `/signin`

Код уже обновлен в `SignInPage.tsx` для создания профиля при входе.

## 📋 Что уже сделано

✅ Обновлен `SignUpPage.tsx`:
- Добавлена задержка для установки сессии
- Улучшена обработка ошибок
- Создание профиля не критично (не блокирует регистрацию)

✅ Обновлен `SignInPage.tsx`:
- Автоматическое создание профиля при входе
- Проверка существования профиля перед созданием

✅ Создана миграция `004_fix_users_insert_policy.sql`:
- Исправляет RLS политику для INSERT

## 🎯 Рекомендуемый порядок действий

1. **Отключите подтверждение email** (Решение 1) - это самое простое
2. **Примените миграцию 004** (Решение 2) - для правильной работы RLS
3. **Перезагрузите страницу** и попробуйте зарегистрироваться снова

## ✅ После исправления

- Регистрация будет работать без ошибок
- Профиль будет создаваться автоматически
- Пользователь сможет использовать Q&A и TeamHub сразу после регистрации

